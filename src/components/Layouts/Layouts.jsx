import React from 'react'
import Header from '../Header/Header'

const layouts = ({Header}) => {
    return (
        <div>
            <Header />
        </div>
    )
}

export default layouts

